apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cerebro
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  project: default
  source:
    repoURL: https://github.com/wiremind/wiremind-helm-charts
    chart: cerebro
    targetRevision: 1.9.4
    # helm:
    #   values: |
    #     elasticsearchHosts: "http://elasticsearch-logging-master:9200"
  destination:
    server: https://kubernetes.default.svc
    namespace: logging